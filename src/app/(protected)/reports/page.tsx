import { requireUser } from "@/lib/auth";import { db } from "@/lib/db";import ReportBuilder from "@/components/report-builder";import Link from "next/link";
export default async function ReportsPage(){const user=await requireUser();const sql=db();const [projects,reports]=await Promise.all([sql.query("SELECT id,code,title,mission_type FROM projects WHERE organization_id=$1 ORDER BY created_at DESC",[user.organization_id]),sql.query(`SELECT r.id,r.title,r.mission_type,r.status,r.current_version,p.code FROM reports r JOIN projects p ON p.id=r.project_id WHERE p.organization_id=$1 ORDER BY r.updated_at DESC LIMIT 50`,[user.organization_id])]);return <div className="content"><div className="hero"><div><div className="eyebrow">Production documentaire</div><h1 className="title">Rapports</h1><p className="subtitle">Génération structurée à partir du dossier et des calculs tracés.</p></div></div><div className="split"><ReportBuilder projects={projects as any}/><div className="card"><h3>Rapports récents</h3><table><tbody>{reports.map((r:any)=><tr key={r.id}><td><Link href={`/reports/${r.id}`}><strong>{r.code}</strong><br/><span className="muted">{r.title}</span></Link></td><td>{r.mission_type}</td><td><span className="badge">V{r.current_version} · {r.status}</span></td></tr>)}</tbody></table>{!reports.length&&<p className="muted">Aucun rapport.</p>}</div></div></div>}
