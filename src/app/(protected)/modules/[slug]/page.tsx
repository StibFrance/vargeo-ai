import { notFound } from "next/navigation";import { getModuleBySlug } from "@/lib/modules";import ModuleWorkbench from "@/components/module-workbench";import { requireUser } from "@/lib/auth";import { db } from "@/lib/db";
export default async function ModulePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const module=getModuleBySlug(slug);if(!module)notFound();const user=await requireUser();const projects=await db().query("SELECT id,code,title FROM projects WHERE organization_id=$1 AND status<>'archived' ORDER BY created_at DESC LIMIT 100",[user.organization_id]);return <div className="content"><div className="hero"><div><div className="eyebrow">{module.code} · {module.title}</div><h1 className="title">{module.short}</h1><p className="subtitle">{module.description}</p><div className="toolbar">{module.standards.map(s=><span className="badge" key={s}>{s}</span>)}</div></div></div><ModuleWorkbench module={module} projects={projects as {id:string;code:string;title:string}[]}/></div>}
