import Link from "next/link";import { MODULES } from "@/lib/modules";
export default function ModulesPage(){return <div className="content"><div className="hero"><div><div className="eyebrow">Moteurs techniques</div><h1 className="title">Modules M1 à M9</h1><p className="subtitle">Chaque exécution enregistre ses entrées, ses résultats, la version du moteur et les références utilisées.</p></div></div><div className="grid grid-3">{MODULES.map(m=><Link className="card module-card" href={`/modules/${m.slug}`} key={m.code}><div><div className="module-code">{m.code}</div><h3>{m.title}</h3><p className="muted">{m.description}</p><div className="toolbar">{m.standards.slice(0,2).map(s=><span className="badge" key={s}>{s}</span>)}</div></div><span className="go">Ouvrir le module →</span></Link>)}</div></div>}
