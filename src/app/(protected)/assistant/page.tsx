import { requireUser } from "@/lib/auth";import { db } from "@/lib/db";import AiAssistant from "@/components/ai-assistant";
export default async function AssistantPage(){const user=await requireUser();const projects=await db().query("SELECT id,code,title FROM projects WHERE organization_id=$1 AND status<>'archived' ORDER BY created_at DESC LIMIT 100",[user.organization_id]);return <div className="content"><div className="hero"><div><div className="eyebrow">Assistant technique</div><h1 className="title">VarGéo.AI Copilot</h1><p className="subtitle">Assistant de synthèse et de contrôle, séparé des moteurs de calcul déterministes.</p></div></div><AiAssistant projects={projects as any}/></div>}
