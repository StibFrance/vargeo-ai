import { NextResponse } from "next/server";import { requireUser } from "@/lib/auth";import { db } from "@/lib/db";
export async function POST(_req:Request,{params}:{params:Promise<{id:string}>}){const user=await requireUser();if(user.role!=="admin"&&user.role!=="engineer")return NextResponse.json({error:"Droit insuffisant"},{status:403});const {id}=await params;const sql=db();const rows=await sql.query(`UPDATE analyses a SET status='validated',validated_by=$1,validated_at=now() FROM projects p WHERE a.id=$2 AND p.id=a.project_id AND p.organization_id=$3 RETURNING a.id`,[user.id,id,user.organization_id]);if(!rows[0])return NextResponse.json({error:"Calcul introuvable"},{status:404});await sql.query("INSERT INTO audit_log(organization_id,user_id,action,entity_type,entity_id) VALUES($1,$2,'validate','analysis',$3)",[user.organization_id,user.id,id]);return NextResponse.json({ok:true});}
